<UserControl x:Class="TestExerciserPro.IViews.TilesExample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:TestExerciserPro.UI.Controls;assembly=TestExerciserPro.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:metroDemo="clr-namespace:TestExerciserPro"
             d:DataContext="{d:DesignInstance metroDemo:MainWindowViewModel}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="LargeTileStyle" TargetType="Controls:Tile">
                <Setter Property="Height" Value="125" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="TitleFontSize" Value="14" />
                <Setter Property="Width" Value="300" />
            </Style>

            <Style x:Key="SmallTileStyle" TargetType="Controls:Tile">
                <Setter Property="Height" Value="125" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="TitleFontSize" Value="10" />
                <Setter Property="Width" Value="147" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled">
        <Grid Background="{DynamicResource AccentColorBrush4}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="430*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="83*" />
                <RowDefinition Height="259*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="1"
                       VerticalAlignment="Center"
                       FontFamily="Segoe UI"
                       FontSize="30"
                       FontWeight="Light"
                       Foreground="{DynamicResource BlackBrush}"
                       Text="Only some Tiles in a WrapPanel" />

            <WrapPanel Grid.Row="1"
                       Grid.Column="1"
                       Width="940"
                       Height="382"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top">
                <Controls:Tile Title="自动化测试"
                               Margin="3"
                               Click="AutoTesting_OnClick"
                               Controls:ControlsHelper.MouseOverBorderBrush="{DynamicResource BlackBrush}"                              
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource LargeTileStyle}">
                    <iconPacks:PackIconModern Width="40"
                                              Height="40"
                                              Kind="PeopleMultiple" />
                </Controls:Tile>
                <Controls:Tile Title="性能测试"
                               Margin="3"
                               Controls:ControlsHelper.MouseOverBorderBrush="{DynamicResource BlackBrush}"
                               Background="Teal"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource LargeTileStyle}">
                    <iconPacks:PackIconFontAwesome Width="40"
                                                   Height="40"
                                                   Kind="DesktopSolid" />
                </Controls:Tile>
                <Controls:Tile Title="接口测试"
                               Background="DimGray"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource LargeTileStyle}">
                    <iconPacks:PackIconModern Width="40"
                                              Height="40"
                                              Kind="Money" />
                </Controls:Tile>
                <Controls:Tile Title="邮件"
                               Background="#D2691E"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource LargeTileStyle}">
                    <iconPacks:PackIconModern Width="40"
                                              Height="40"
                                              Kind="Email" />
                </Controls:Tile>
                <Controls:Tile Title="思维导图"
                               Background="Green"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource LargeTileStyle}">
                    <iconPacks:PackIconEntypo Width="40"
                                              Height="40"
                                              Kind="News" />
                </Controls:Tile>
                <Controls:Tile Title="笔记本"
                               Background="#FF842D"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource SmallTileStyle}">
                    <iconPacks:PackIconMaterial Width="40"
                                                Height="40"
                                                Kind="Notebook" />
                </Controls:Tile>
                <Controls:Tile Title="设置"
                               Background="#1E90FF"
                               HorizontalTitleAlignment="Right"
                               Style="{StaticResource SmallTileStyle}">
                    <iconPacks:PackIconMaterial Width="40"
                                                Height="40"
                                                Kind="Settings" />
                </Controls:Tile>
            </WrapPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
