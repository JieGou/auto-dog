<UserControl x:Class="AutoDog.Views.APIFileTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
             xmlns:Controls="clr-namespace:AutoDog.UI.Controls;assembly=AutoDog.UI"
             xmlns:local="clr-namespace:AutoDog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="请求方式：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <ListBox Grid.Row="0" Grid.Column="1" Margin="3,5,0,5" RenderTransformOrigin="0.5,0.5">
        </ListBox>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="请求URL：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="3,5,0,5"></TextBox>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="请求URL：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="3,5,0,5"></TextBox>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="请求URL：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="3,5,0,5"></TextBox>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="请求URL：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="3,5,0,5"></TextBox>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="请求URL：" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="3,5,0,5"></TextBox>

    </Grid>
</UserControl>
